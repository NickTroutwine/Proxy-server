{
  "name": "proxy-server",
  "version": "0.1.0",
  "description": "example proxy server",
  "main": "proxy server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nick Troutwine",
  "license": "ISC",
  "dependencies": {
    "bouncy": "^3.2.2",
    "portscanner": "^1.0.0"
  }
}
